node_modules

# ayjnt generated artifacts. Only .ayjnt/migrations.json is committed —
# everything else regenerates on every `ayjnt build`.
.ayjnt/*
!.ayjnt/migrations.json

.wrangler
.env
.env.local

# ayjnt compile output
/notes-app

# created by the appendToLog host tool when you run the example
/notes.log
