# CatalogAgent

Hosts the React UI that reads `/__ayjnt/catalog` and renders every
agent the caller can reach. Open `/catalog/me` in a browser; type
`letmein` into the bearer-token field to unlock the admin agents.
